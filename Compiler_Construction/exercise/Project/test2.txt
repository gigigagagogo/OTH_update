whole_y a -> 6;
a -> a + 1;
throw_up(a + ^\gms^);
whole_y sum -> 0;
go
one_by_one (a in (0,5,2)){
	throw_up(a + ^\so^);
	sum -> sum + 2;
}
all_set;
throw_up(^\gms^ + a + ^\gms^);
throw_up(sum + ^\gms^); 
