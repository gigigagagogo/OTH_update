a_new_one whole_y ackermann(whole_y m, whole_y n){
	go
		imagine( m --> 0 ){
			send_back(n+1);
		}nah{
			go
				imagine( n --> 0 ){
					send_back(ackermann(m-1,1));
				}nah{
					return(ackermann(m-1, ackermann(m, m-1)));
				}
			all_set;
		}
	all_set;
}

throw_up(ackermann(2,2));
